# Artifact registry - published claude.ai snapshots of HQ deliverables
<!-- LEDGER. One line per publish event, written going forward: "- YYYY-MM-DD HH:MM AM/PM — {Department}: {description} — {url} — source: {path}" (em-dash-delimited - a hyphen separator is ambiguous since descriptions and paths both contain hyphens). Earlier shapes: "- {path} -> {url} ({date})" (born 2026-07-23) and "- {date} {path} -> {url} ({context})" (2026-07-24 bulk publish) stand as history, never rewritten. A republish reuses its path's existing URL. "CAP-QUEUED" replaces the URL when the platform's daily publish cap defers. Latest line per path is current; earlier lines are never edited or deleted. Disk pairs stay canonical - artifacts are snapshots. -->
